---
# community.general provides the ufw/timezone modules; ansible.posix provides authorized_key.
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general
  - name: ansible.posix
