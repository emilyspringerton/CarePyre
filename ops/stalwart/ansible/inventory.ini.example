# Copy to inventory.ini (gitignored -- real IP/hostname never committed) and fill in the new,
# separate Linode instance's real address once it exists. Per the 2026-09-05 founder decision
# (docs/STALWART_HOSTING_DECISION.md), this is a genuinely new, isolated box -- NOT the shared
# farthq.com/iduna.farthq.com/carepyre.org box this monorepo's other services already run on.

[stalwart]
mail.carepyre.org ansible_host=203.0.113.10 ansible_user=root

[stalwart:vars]
ansible_ssh_private_key_file=~/.ssh/id_ed25519
